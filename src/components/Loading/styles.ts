import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  loadingButton: {
    zIndex: 9999,
    position: 'absolute',
    backgroundColor: '#000',
    height: '100%',
    width: '100%',
    opacity: 0.5,
  },
});
