export interface LocationType {
  latitude: number;
  longitude: number;
}
export interface RegionType {
  latitude: number;
  longitude: number;
  latitudeDelta: number;
  longitudeDelta: number;
  address?: string;
}
